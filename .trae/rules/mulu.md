- 文件夹名称采用小写字母，单词之间用下划线分隔。
- 文件名称采用大驼峰命名法，单词之间不使用下划线。
- 所有的不变的变量都必须使用`const`关键字声明，并且采用大驼峰命名法。
- 所有的模块都应该考虑解耦，每个模块都应该只负责自己的功能，而不是依赖其他模块。适当的添加中间层来处理未来可能的变化，例如在使用数据的存储方案时，要考虑到未来可能会使用A方案或B方案，那么要么需要注入A和B两个方案的增删改查，并且在中间层进行切换，在使用时直接调用中间层的方法即可。
